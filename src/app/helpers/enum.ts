export enum Jonour {
    Basic = 0,
    Public = 1,
    Private = 2,
}

export enum Complexity {
    Stable = 0,
    Mild = 1,
    Cevoiur = 2,
}

export enum Status {
    Submitted = 0,
    Assigned = 1,
    Active = 2,
    Solved = 3,
    Pending = 4,
}
